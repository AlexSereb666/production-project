{
    "compilerOptions": {
      // куда производится сборка //
      // не важна, если собирается с помощью webpack //
      "outDir": "./dist/",
      // подсвечивает все места где не указан тип //
      // однако не запрещает использовать any //
      "noImplicitAny": true,
      "module": "esnext", // было - es5, но из-за него не работает адаптивный импорт (ленивая подгрузка)
      // в какую спецификацию компилируем - для поддержки большинства браузеров //
      "target": "es5",
      // чтобы не просило каждый раз импортировать react, ставим react-jsx //
      "jsx": "react-jsx",
      // компилятор будет обрабатывать не только TS файлы, но и JS файлы //
      "allowJs": true,
      "moduleResolution": "node",
      // для абсолютного импорта //
      "baseUrl": ".",
      // сокращение больших импортов //
      "paths": {
        "*": ["./src/*"]
      },
      // esModuleInterop позволяет работать с пакетами, которые используют //
      // common js как с обычными пакетами с помощью import //
      "esModuleInterop": true,
      // allowSyntheticDefaultImports - делает импорты более привычными //
      // дефолтный импорт по умолчанию не поддерживается и приходится писать //
      // import * as Name from '....' //
      // с true можем писать import Name from '....' //
      "allowSyntheticDefaultImports": true,
    },
    // Для того, чтобы импорты корректно работали //
    "ts-node": {
      "compilerOptions": {
        "module": "CommonJS"
      }
    },
    "include": [
      "./config/jest/setupTests.ts"
    ]
  }